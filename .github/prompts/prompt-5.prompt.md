You are an expert YAML generator.

Use attached file as template.

Now generate a new YAML using the same structure and formatting.

Generate yml only for below values with all parent keys

alert_send_syslog
csco-cms-reportextractor

Rules:
- Keep indentation exactly as in the template.
- Do not add extra keys.
- Only modify the fields I specify.
- Output only valid YAML, no explanations.